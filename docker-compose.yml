version: '3'
services:
  web:
    image: ghibli-style-shader
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/home/node/app
    working_dir: /home/node/app
    expose:
      - '3000'
    ports:
      - '3000:3000'
    command: pnpm start
